/* styles.css */

body {
    font-family: 'Press Start 2P', Arial, sans-serif; /* Use a pixel font similar to Stardew Valley */
    background-color: #f5f5dc; /* A soft, light color to match the game’s background */
    color: #000; /* Standard black text */
    padding: 20px;
    display: flex;
    flex-direction: column;
    align-items: center;
}

.achievement {
    background: url('assets/panel_background.png') no-repeat center center; /* Use a background image similar to in-game panels */
    border: 2px solid #8b4513; /* Brown border */
    padding: 10px;
    margin: 10px;
    width: 300px;
    box-shadow: 3px 3px 8px rgba(0, 0, 0, 0.3); /* Slight shadow for depth */
    text-align: center;
}

.achievement-icon {
    cursor: pointer;
    display: flex;
    flex-direction: column;
    align-items: center;
}

.achievement-icon img {
    width: 64px; /* Adjust size as necessary */
    height: 64px;
}

.achievement-icon p {
    margin: 5px 0 0 0;
}

.achievement-details {
    display: none;
    padding-top: 10px;
}

.category {
    margin: 10px 0;
}

.category-header {
    cursor: pointer;
    background-color: #deb887; /* A beige color similar to in-game UI elements */
    padding: 5px;
    border-radius: 5px;
}

.category-details {
    display: none;
    padding-top: 5px;
}

.item {
    display: flex;
    flex-direction: column;
    align-items: center;
    margin: 5px 0;
    cursor: pointer;
    background-color: #fff5d1; /* Light yellowish color */
    border: 1px solid #d1b08b; /* Light brown border */
    padding: 5px;
    border-radius: 5px;
}

.item img {
    width: 48px;
    height: 48px;
    margin-bottom: 5px;
}

.item.completed img {
    filter: grayscale(100%);
    opacity: 0.5;
}

.item p {
    margin: 0;
    font-size: 12px; /* Smaller text for details */
    text-align: center;
}
